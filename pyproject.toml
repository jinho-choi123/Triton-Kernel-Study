[project]
name = "triton-kernel-study"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "jupyterlab>=4.5.1",
    "numpy<2.0.0",
    "torch>=2.9.1",
    "triton>=3.5.1",
]
